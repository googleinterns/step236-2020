// @flow

export type UserType = {
  adminNote: string,
  email: string,
  isAdmin: boolean,
  joinDate: string,
  name: string,
  needsAttention: false,
  partnerEmail: string,
  count: number,
  id: string
};
